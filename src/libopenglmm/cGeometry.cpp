// Standard headers
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <cassert>
#include <cmath>

#include <string>
#include <iostream>
#include <sstream>
#include <map>
#include <vector>

// libopenglmm headers
#include <libopenglmm/cGeometry.h>

namespace opengl
{
  void cGeometryBuilder::CreatePlane(std::vector<float>& vertices, std::vector<float>& normals)
  {
  }

  void cGeometryBuilder::CreatePlane(std::vector<float>& vertices, std::vector<float>& normals, std::vector<float>& textureCoords, size_t nTextureUnits)
  {
  }

  void cGeometryBuilder::CreateBox(std::vector<float>& vertices, std::vector<float>& normals)
  {
  }

  void cGeometryBuilder::CreateBox(std::vector<float>& vertices, std::vector<float>& normals, std::vector<float>& textureCoords, size_t nTextureUnits)
  {
  }
}
